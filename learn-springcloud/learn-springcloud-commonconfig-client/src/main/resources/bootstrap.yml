spring:
  profiles:
    active: dev

---
spring:
  profiles: prod
  application:
    name: learn-springcloud-commonconfig-client
  cloud:
     config:
       uri: http://localhost:10000
       label: master
       profile: prod


---
spring:
  profiles: dev
  application:
    name: learn-springcloud-commonconfig-client
  cloud:
     config:
       uri: http://localhost:10000
       label: master
       profile: dev